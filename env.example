# TaskForge HITL – Environment Variables Example

# PostgreSQL connection string
DATABASE_URL=

# Enable Flask debug logs locally if needed
FLASK_DEBUG=false

# Flask runtime configuration
FLASK_ENV=production

# Railway (Nixpacks) start override (optional locally)
# NIXPACKS_START_CMD=gunicorn -w 4 -b 0.0.0.0:$PORT app.server:app 